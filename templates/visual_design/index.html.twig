{% extends 'base.html.twig' %}

{% block title %}CitadelQuest Visual Design{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="glass-panel p-4 mb-4">
                <h1 class="text-light mb-4">CitadelQuest Visual Design</h1>
                <p class="mb-4">
                    A comprehensive guide to CitadelQuest's immersive visual design system.
                    This documentation helps maintain consistency across the platform while
                    delivering an engaging, game-like experience.
                </p>
            </div>
        </div>
    </div>

    <div class="row g-4">
        {# Fonts #}
        <div class="col-md-6">
            <div class="glass-panel p-4 h-100">
                <h2 class="text-light">Typography</h2>
                <p class="">
                    Nunito font family with weights 300, 400, 500, and 700.
                    Optimized for both readability and aesthetic appeal.
                </p>
                <a href="{{ path('app_visual_design_fonts') }}" class="btn btn-sm btn-light mt-3">View Fonts</a>
            </div>
        </div>

        {# Colors #}
        <div class="col-md-6">
            <div class="glass-panel p-4 h-100">
                <h2 class="text-light">Color System</h2>
                <p class="">
                    Our signature green (#95ec86) combined with a carefully
                    crafted dark theme palette for optimal contrast and immersion.
                </p>
                <a href="{{ path('app_visual_design_colors') }}" class="btn btn-sm btn-light mt-3">View Colors</a>
            </div>
        </div>

        {# Background #}
        <div class="col-md-6">
            <div class="glass-panel p-4 h-100">
                <h2 class="text-light">Background System</h2>
                <p class="">
                    Multi-layered background combining custom artwork with
                    dynamic SVG patterns and glassmorphism effects.
                </p>
                <a href="{{ path('app_visual_design_background') }}" class="btn btn-sm btn-light mt-3">View Background</a>
            </div>
        </div>

        {# Components #}
        <div class="col-md-6">
            <div class="glass-panel p-4 h-100">
                <h2 class="text-light">Components</h2>
                <p class="">
                    Reusable UI components featuring glass effects, consistent
                    styling, and smooth animations.
                </p>
                <a href="{{ path('app_visual_design_components') }}" class="btn btn-sm btn-light mt-3">View Components</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
