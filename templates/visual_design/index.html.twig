{% extends 'base.html.twig' %}

{% block title %}CitadelQuest Visual Design{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="glass-panel_ p-4 mb-4">
                <h1 class="mb-4">CitadelQuest Visual Design</h1>
                <p class="mb-4">
                    A comprehensive guide to CitadelQuest's immersive visual design system.
                    This documentation helps maintain consistency across the platform while
                    delivering an engaging, game-like experience.
                </p>
            </div>
        </div>
    </div>

    <div class="row">
        {# Fonts #}
        <div class="col-12 col-md-6 mb-4">
            <div class="glass-panel p-4 h-100">
                <div class="card-header">
                    <h2>Typography</h2>
                </div>
                <div class="card-body body-color">
                    <p class="">
                        Nunito font family with weights 100, 300, 500, and 700.
                        Optimized for both readability and aesthetic appeal.
                    </p>
                    <a href="{{ path('app_visual_design_fonts') }}" type="button" class="btn btn-sm btn-light float-end">View Fonts</a>
                    <div class="clearfix"></div>
                </div>
            </div>
        </div>

        {# Colors #}
        <div class="col-12 col-md-6 mb-4">
            <div class="glass-panel p-4 h-100">
                <div class="card-header">
                    <h2>Color System</h2>
                </div>
                <div class="card-body body-color">
                    <p class="">
                        Our signature green (#95ec86) combined with a carefully
                        crafted dark theme palette for optimal contrast and immersion.
                    </p>
                    <a href="{{ path('app_visual_design_colors') }}" type="button" class="btn btn-sm btn-light float-end">View Colors</a>
                    <div class="clearfix"></div>
                </div>
            </div>
        </div>

        {# Background #}
        <div class="col-12 col-md-6 mb-4">
            <div class="glass-panel p-4 h-100">
                <div class="card-header">
                    <h2>Background System</h2>
                </div>
                <div class="card-body body-color">
                    <p class="">
                        Multi-layered background combining custom artwork with
                        dynamic SVG patterns and glassmorphism effects.
                    </p>
                    <a href="{{ path('app_visual_design_background') }}" type="button" class="btn btn-sm btn-light float-end">View Background</a>
                    <div class="clearfix"></div>
                </div>
            </div>
        </div>

        {# Components #}
        <div class="col-12 col-md-6 mb-4">
            <div class="glass-panel p-4 h-100">
                <div class="card-header">
                    <h2>Components</h2>
                </div>
                <div class="card-body body-color">
                    <p class="">
                        Reusable UI components featuring glass effects, consistent
                        styling, and smooth animations.
                    </p>
                    <a href="{{ path('app_visual_design_components') }}" type="button" class="btn btn-sm btn-light float-end">View Components</a>
                    <div class="clearfix"></div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
