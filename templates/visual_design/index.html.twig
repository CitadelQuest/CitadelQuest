{% extends 'base.html.twig' %}

{% block title %}CitadelQuest Visual Design{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="glass-panel_ p-4 mb-4">
                <h1 class="mb-4">CitadelQuest Visual Design</h1>
                <p class="mb-4">
                    A comprehensive guide to CitadelQuest's immersive visual design system.
                    This documentation helps maintain consistency across the platform while
                    delivering an engaging, game-like experience.
                </p>
            </div>
        </div>
    </div>

    <div class="row">
        {# Fonts #}
        <div class="col-12 col-md-6 mb-4">
            <div class="glass-panel h-100">
                <div class="card-header bg-cyber-g border-success border-1 border-bottom px-4 pt-3 pb-1 rounded-top">
                    <h4>Typography</h4>
                </div>
                <div class="card-body body-color px-4 pt-2 pb-3">
                    <p class="">
                        Nunito font family with weights 100, 300, 500, and 700.
                        Optimized for both readability and aesthetic appeal.
                    </p>
                    <p class="mt-2 text-center">
                        <a href="{{ path('app_visual_design_fonts') }}" type="button" class="btn btn-sm btn-light">View Fonts</a>
                    </p>
                </div>
            </div>
        </div>

        {# Colors #}
        <div class="col-12 col-md-6 mb-4">
            <div class="glass-panel h-100">
                <div class="card-header bg-cyber-g border-success border-1 border-bottom px-4 pt-3 pb-1 rounded-top">
                    <h4>Color System</h4>
                </div>
                <div class="card-body body-color px-4 pt-2 pb-3">
                    <p class="">
                        Our signature green (#95ec86) combined with a carefully
                        crafted dark theme palette for optimal contrast and immersion.
                    </p>
                    <p class="mt-2 text-center">
                        <a href="{{ path('app_visual_design_colors') }}" type="button" class="btn btn-sm btn-light">View Colors</a>
                    </p>
                </div>
            </div>
        </div>

        {# Background #}
        <div class="col-12 col-md-6 mb-4">
            <div class="glass-panel h-100">
                <div class="card-header bg-cyber-g border-success border-1 border-bottom px-4 pt-3 pb-1 rounded-top">
                    <h4>Background System</h4>
                </div>
                <div class="card-body body-color px-4 pt-2 pb-3">
                    <p class="">
                        Multi-layered background combining custom artwork with
                        dynamic SVG patterns and glassmorphism effects.
                    </p>
                    <p class="mt-2 text-center">
                        <a href="{{ path('app_visual_design_background') }}" type="button" class="btn btn-sm btn-light">View Background</a>
                    </p>
                </div>
            </div>
        </div>

        {# Components #}
        <div class="col-12 col-md-6 mb-4">
            <div class="glass-panel h-100">
                <div class="card-header bg-cyber-g border-success border-1 border-bottom px-4 pt-3 pb-1 rounded-top">
                    <h4>Components</h4>
                </div>
                <div class="card-body body-color px-4 pt-2 pb-3">
                    <p class="">
                        Reusable UI components featuring glass effects, consistent
                        styling, and smooth animations.
                    </p>
                    <p class="mt-2 text-center">
                        <a href="{{ path('app_visual_design_components') }}" type="button" class="btn btn-sm btn-light">View Components</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
