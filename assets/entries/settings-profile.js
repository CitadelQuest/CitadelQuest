import { SettingsProfileManager } from '../js/features/settings/components/SettingsProfileManager.js';

// Initialize profile manager when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    new SettingsProfileManager();
});