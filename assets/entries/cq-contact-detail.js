import { ContactDetailManager } from '../js/features/cq-contact/ContactDetailManager.js';

// Initialize when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
    window.contactDetailManager = new ContactDetailManager();
});
